{
  "name": "chatgpt-api-web",
  "version": "1.2.0",
  "description": "Hardened local OpenAI-compatible API backed by an authenticated ChatGPT Web session via Chrome CDP and Playwright.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "init-session": "node init-session.js",
    "check": "node --check index.js && node --check init-session.js && node --check openai-test.js && node --check test.js && node --check lib/config.js && node --check lib/security.js && node --check lib/store.js && node --check lib/logger.js && node --check lib/browser.js && node --check test/security.test.js && node --check test/store.test.js",
    "test": "npm run check && node test/security.test.js && node test/store.test.js",
    "test:integration": "node test.js",
    "test:openai": "node openai-test.js",
    "audit": "npm audit --omit=dev --audit-level=high"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "5.2.1",
    "playwright": "1.62.1"
  },
  "keywords": [
    "chatgpt",
    "openai-compatible",
    "api",
    "playwright",
    "chrome",
    "cdp",
    "sse",
    "local",
    "security"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mileadev/chatgpt-api-web.git"
  },
  "license": "MIT"
}
